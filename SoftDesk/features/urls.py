from rest_framework import routers
from .views import ProjectViewSet, IssuesViewSet, CommentsViewSet


# router = routers.DefaultRouter()
# router.register('api/projects', ProjectViewSet, basename='projects')
# router.register('api/issues', IssuesViewSet, basename='issues')
# router.register('api/comments', CommentsViewSet, basename='comments')
